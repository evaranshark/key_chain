name: key_chain
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.7.0
  go_router: ^13.2.4
  surf_lint_rules: ^3.0.0
  generator:
    path: lib/features/generator
  equatable: ^2.0.5
  logger: ^2.5.0
  drift: ^2.24.0
  drift_flutter: ^0.2.4
  flutter_bloc: ^9.0.0

dev_dependencies:
  build_runner: ^2.4.14
  drift_dev: ^2.24.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  test: ^1.25.8

flutter:
  uses-material-design: true
